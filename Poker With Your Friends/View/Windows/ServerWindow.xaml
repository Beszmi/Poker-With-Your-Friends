<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Poker_With_Your_Friends.ServerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poker_With_Your_Friends"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Model="using:Poker_With_Your_Friends.Model"
    mc:Ignorable="d"
    Title="ServerWindow"
    Closed="Window_Closed">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Text="Server Window" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Margin="10"/>
        <StackPanel Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <ListView ItemsSource="{x:Bind viewModel.game.PlayerNames, Mode=OneWay}" SelectedItem="{x:Bind viewModel.SelectedPlayerName, Mode=TwoWay}" Margin="10" MaxHeight="200" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10" Margin="15">
                <Button Content="Delete Player" Click="DeletePlayerButton_Click" Margin="10"/>
                <Button Content="Edit Player" Click="EditPlayerButton_Click" Margin="10"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Spacing="5" Margin="15" Visibility="{x:Bind EditPanelVisible, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Username: "/>
                    <TextBox Text="{x:Bind viewModel.NewName, Mode=TwoWay}" PlaceholderText="{x:Bind viewModel.SelectedPlayerName, Mode=OneWay}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Chips: "/>
                    <TextBox Text="{x:Bind viewModel.NewChips, Mode=TwoWay}" PlaceholderText="{x:Bind viewModel.SelectedPlayerChips, Mode=OneWay}"/>
                </StackPanel>
                <Button Content="Done" Click="EditDoneButton_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
        <Border Grid.Column="1" Grid.Row="1" Background="White" MaxHeight="500">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                <ItemsControl ItemsSource="{x:Bind viewModel.ServerLogs, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{x:Bind}" 
                           FontFamily="Consolas" 
                           FontSize="13" 
                           Foreground="Black"
                           Margin="0,2" 
                           TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <Button Grid.Column="2" Grid.Row="1" Content="Debug messages flip" Click="DebugMode_Click" VerticalAlignment="Center" HorizontalAlignment="Center"></Button>
    </Grid>
</Window>
